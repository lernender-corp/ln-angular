import { Color } from './color';

export class InteriorColor extends Color {
  /*
   * Constructor()
   */
  constructor();

  constructor(options: object);
  constructor(options?: any) {
    super(options);
  }
}
